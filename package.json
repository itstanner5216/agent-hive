{
  "name": "agent-hive",
  "version": "1.1.0",
  "private": true,
  "description": "Context-Driven Development for AI coding assistants",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter pantheon-core build && bun run --filter opencode-pantheon build && bun run --filter vscode-hive build",
    "dev": "bun run --filter pantheon-core build && bun run --filter '*' dev",
    "release:prepare": "node scripts/release/prepare.mjs",
    "release:check": "bun install && bun run build && npm -ws --if-present run test"
  },
  "license": "MIT"
}
